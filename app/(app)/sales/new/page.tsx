import { ComposeSalesInvoice } from '@/components/screens/ComposeSalesInvoice';

/** Thin adapter — see the note on the week view route. */
export default function Page() {
  return <ComposeSalesInvoice />;
}
